[pytest]
# Permite importar el paquete `app` desde las pruebas (raíz = backend/).
pythonpath = .
# Acota la recolección a backend/tests/ para NO recoger test_register.py
# (script manual que hace llamadas reales y requiere entorno).
testpaths = tests
