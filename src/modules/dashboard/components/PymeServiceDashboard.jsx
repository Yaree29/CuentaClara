//componente para el dashboard de PYME de servicios, con reportes financieros y de ventas, sin modulo de inventario

import React from 'react';
import { View, Text } from 'react-native';

const PymeServiceDashboard = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Dashboard PYME Servicios (En construcción)</Text>
    </View>
  );
};

export default PymeServiceDashboard;