import { StyleSheet } from "react-native";

export default StyleSheet.create({
  // === CONTENEDOR SECCIÓN ===
  container: {
    marginBottom: 16,
  },

  // === LISTA DE METAS ===
  list: {
    gap: 12, // Mantiene una separación constante entre tarjetas de progreso
  },
});